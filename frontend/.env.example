# Leave empty for local dev (uses the Vite proxy to localhost:4000).
# In production set this to your deployed backend URL, e.g.:
# VITE_API_BASE_URL=https://hindustan-finserve-api.onrender.com
VITE_API_BASE_URL=
